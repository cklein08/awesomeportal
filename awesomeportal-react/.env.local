# Local env for development (do NOT commit secrets)
VITE_ADOBE_CLIENT_ID=ko-assets-ims
# Optionally override other VITE_ values here, e.g. VITE_BUCKET, VITE_EXC_BASE_URL

# Heineken-style portal demo (skin + grid banner / strip)
VITE_HEINEKEN_DEMO=true
