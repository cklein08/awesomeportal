// Local development configuration override
// Copy this file to config.local.js (which is gitignored) and customize as needed

window.awesomeportalConfig = {
  externalParams: {
    restrictedBrands: [
      { title: 'Coca-Cola', value: 'coca-cola' },
      { title: 'Sprite', value: 'sprite' }
    ],
    excFacets: {
      // Add your facet configuration here if needed
    },
    accordionTitle: 'My Custom Title',
    accordionContent: 'My custom accordion content',
    isBlockIntegration: false,
    // Optional: grid slot blocks from DA live or any embedder. When non-empty, replaces default tiles.
    // Each item: { id, title, description?, iconUrl?, href?, appId? }. appId 'firefly'|'experience-hub'|'ai-agents' uses built-in app.
    // slotBlocks: [
    //   { id: 'firefly', title: 'Adobe Firefly', description: 'Generate images using AI', appId: 'firefly' },
    //   { id: 'custom', title: 'Custom Link', description: 'Open external site', href: 'https://example.com', iconUrl: 'https://example.com/icon.png' }
    // ]
  }
};
