/** In-portal Files rail: show Adobe Files inside the main workspace iframe (same window as the grid). */
export const ADOBE_FILES_WEB_URL = 'https://www.adobe.com/files';

/** Synthetic `selectedAppId` — not a left-nav app row; handled in MainApp content branch. */
export const PORTAL_EMBED_ADOBE_FILES_APP_ID = 'adobe-cloud-files';
