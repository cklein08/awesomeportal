/** Starter prompts for the portal workspace agent UI (shared with Admin activities). */
export const PORTAL_AGENT_MODEL_PROMPTS = [
    'Summarize this layout',
    'Suggest tiles for marketeers',
    'List empty slots',
    'Explain persona nav',
] as const;
