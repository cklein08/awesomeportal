{
  "name": "@awesomeportal/cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for KO Assets",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "echo SKIPPED: No tests specified. For Cloudflare workers we might want to look into https://developers.cloudflare.com/workers/testing/",
    "predev": "./scripts/local-secret-store.sh",
    "dev": "wrangler dev",
    "deploy": "./scripts/deploy.sh",
    "tail": "wrangler tail",
    "lint": "biome lint",
    "lint:fix": "biome lint --fix",
    "lint-ci": "biome ci --reporter=github || biome lint",
    "format": "biome format",
    "format:fix": "biome format --fix"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@types/react-dom": "^19.2.2",
    "wrangler": "^4.38.0"
  },
  "dependencies": {
    "itty-router": "^5.0.22",
    "jose": "^6.0.13"
  }
}
